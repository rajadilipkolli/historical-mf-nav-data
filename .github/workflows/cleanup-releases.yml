name: Cleanup Old Releases

on:
  schedule:
    # Run weekly on Sundays at midnight UTC
    - cron: '0 0 * * 0'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required permission to delete releases and tags
      packages: write # Required permission to delete container images
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Delete Releases and Tags Older Than 15 Days
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Calculate the date 15 days ago in ISO 8601 format
          THRESHOLD_DATE=$(date -d "15 days ago" +%Y-%m-%dT%H:%M:%SZ)
          echo "Deleting releases published before: $THRESHOLD_DATE"
          
          # Fetch releases and filter those older than 15 days using jq
          gh release list --limit 1000 --json tagName,publishedAt -q ".[] | select(.publishedAt < \"$THRESHOLD_DATE\") | .tagName" > old_releases.txt
          
          if [ -s old_releases.txt ]; then
            echo "Found the following old releases/tags to delete:"
            cat old_releases.txt
            
            # Loop through and delete the release, artifacts, and the tag itself
            while read -r tag; do
              echo "Deleting release and tag: $tag"
              gh release delete "$tag" -y --cleanup-tag
            done < old_releases.txt
          else
            echo "No releases older than 15 days found."
          fi

      - name: Delete old GHCR images
        uses: snok/container-retention-policy@v3.0.0
        with:
          account-type: personal
          account: rajadilipkolli
          token: ${{ secrets.GITHUB_TOKEN }}
          image-names: historical-mf-nav-data
          cut-off: 15 days ago UTC
          keep-n-most-recent: 3
